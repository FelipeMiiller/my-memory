# Variáveis de ambiente locais com credenciais reais (não commitar no Git)
*.local
*.env.local

# Permite o .env padrão do repositório (comentado para SQLite)
!.env

# Bancos de dados SQLite locais
*.db
*.db-journal
*.db-wal
*.db-shm
