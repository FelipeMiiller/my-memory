/**
 * IPC router — placeholder.
 *
 * Phase 1 stubs return rejected promises directly from `ipcMain.handle()`
 * in main.ts. Phase 2 ADR-049 promotes this to typed channel routing.
 */
export const router = {};