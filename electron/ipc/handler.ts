/**
 * RPC handler — placeholder.
 *
 * Phase 1 does not use ORPC. Phase 2 ADR-049 may introduce typed RPC if
 * the IPC surface grows beyond a handful of channels.
 */
export const rpcHandler = undefined;
